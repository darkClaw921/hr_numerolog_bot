"""
Слой персистентности на SQLAlchemy 2.0 (async) + SQLite.

Схема спроектирована как общий контракт: будущий отдельный бот совместимости
читает те же таблицы (прежде всего `people`) из того же файла БД.
"""
